"use strict";




var _chunkKA6EMQBDjs = require('./chunk-KA6EMQBD.js');

// src/cli.ts
var _cac = require('cac');
var name = "unocss";
async function main(options = {}) {
  const cli = _cac.cac.call(void 0, name);
  cli.command("[...patterns]", "Glob patterns", {
    ignoreOptionDefaultValue: true
  }).option("-o, --out-file <file>", "Output file", {
    default: process.cwd()
  }).option("-w, --watch", "Watch for file changes").action(async (patterns, flags) => {
    Object.assign(options, _chunkKA6EMQBDjs.__spreadValues.call(void 0, {}, flags));
    if (patterns)
      options.patterns = patterns;
    await _chunkKA6EMQBDjs.build.call(void 0, options);
  });
  cli.help();
  cli.version(_chunkKA6EMQBDjs.version);
  cli.parse(process.argv, { run: false });
  await cli.runMatchedCommand();
}
main().catch(_chunkKA6EMQBDjs.handleError);
