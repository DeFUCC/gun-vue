{
  "name": "@gun-vue/repo",
  "private": true,
  "version": "0.1.2",
  "funding": {
    "type": "ko-fi",
    "url": "https://ko-fi.com/B0B44CM90"
  },
  "homepage": "https://defucc.github.io/gun-vue",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "demo": "cd demo && pnpm run dev",
    "site": "cd site && pnpm run dev",
    "build": "cd composables && pnpm run build && cd .. && cd components && pnpm run build && cd .. && cd demo && pnpm run build && cd .. && cd site && pnpm run build",
    "relay": "cd relay && pnpm run start",
    "deploy": "cd demo && pnpm run deploy",
    "ch": "pnpm changeset",
    "v": "pnpm changeset version",
    "publish": "cd composables && pnpm run build && cd .. && cd components && pnpm run build && cd .. && pnpm changeset publish",
    "doc": "jsdoc -c jsdoc.json"
  },
  "devDependencies": {
    "@changesets/cli": "^2.19.0",
    "jsdoc": "^3.6.7",
    "minami": "^1.2.3",
    "pug": "^3.0.2"
  }
}
