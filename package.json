{
  "name": "@gun-vue/repo",
  "private": true,
  "version": "0.1.2",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "dev": "cd demo && pnpm run dev",
    "lib": "cd composables && pnpm run build",
    "comp": "cd components && pnpm run build",
    "demo": "cd demo && pnpm run build",
    "site": "cd site && pnpm run build",
    "start": "cd relay && pnpm run start",
    "deploy": "cd demo && pnpm run deploy",
    "ch": "pnpm changeset",
    "v": "pnpm changeset version",
    "publish": "pnpm changeset publish",
    "doc": "jsdoc -c jsdoc.json"
  },
  "devDependencies": {
    "@changesets/cli": "^2.18.1",
    "jsdoc": "^3.6.7",
    "minami": "^1.2.3"
  }
}
