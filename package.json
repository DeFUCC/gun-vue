{
  "name": "@gun-vue/repo",
  "private": true,
  "version": "0.6.4",
  "funding": {
    "type": "ko-fi",
    "url": "https://ko-fi.com/B0B44CM90"
  },
  "homepage": "https://defucc.github.io/gun-vue",
  "engines": {
    "node": "16.x"
  },
  "scripts": {
    "demo": "cd demo && pnpm run dev",
    "docs": "cd docs && pnpm run dev",
    "build": "cd composables && pnpm run build && cd .. && cd components && pnpm run build && cd .. && cd docs && pnpm run build && cd .. && cd demo && pnpm run build ",
    "change": "pnpm changeset",
    "version": "pnpm changeset version",
    "publish": "pnpm run build && pnpm changeset publish",
    "deploy": "sh deploy.sh",
    "relay": "cd relay && pnpm run start"
  },
  "devDependencies": {
    "@changesets/cli": "^2.20.0"
  }
}
